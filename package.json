{
  "name": "mentor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js", // Script to start the server in production
    "dev": "nodemon index.js", // Script to run the server in development using nodemon
    "test": "jest" // Script to run tests (if using Jest for testing)
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  }
}